/** 
 * Declaração de tipo para o objeto de configuração do Next.js.
 * Isso permite autocomplete e validação de tipos ao configurar o Next.js.
 */
const nextConfig = {} // Objeto de configuração do Next.js. Pode ser usado para definir opções como redirects, rewrites, env, etc.

// Exporta o objeto de configuração para que o Next.js utilize ao iniciar o projeto.
// Esse arquivo é lido automaticamente pelo Next.js para aplicar configurações personalizadas.
module.exports = nextConfig
