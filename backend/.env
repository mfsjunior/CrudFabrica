MONGO_URI=mongodb+srv://<usuario>:<senha>@cluster0.datgvgs.mongodb.net/sdd_crud?retryWrites=true&w=majority
# MONGO_URI: Define a string de conexão do MongoDB que será usada pelo backend.
# Essa variável é lida pelo NestJS (via ConfigModule) para conectar ao banco de dados.
# O valor inclui usuário, senha, endereço do cluster, nome do banco (sdd_crud) e opções de conexão.