"use strict"; // Ativa o modo estrito do JavaScript para evitar erros comuns.

// Define a propriedade __esModule para compatibilidade com módulos ES6.
// Isso permite que o arquivo seja importado corretamente em outros módulos.
Object.defineProperty(exports, "__esModule", { value: true });

// Exporta a classe CreateItemDto para ser usada em outros arquivos.
exports.CreateItemDto = void 0;

// Define a classe CreateItemDto, que representa o Data Transfer Object (DTO) para criação de itens.
// No TypeScript original, essa classe define os campos esperados ao criar um item.
// Aqui, ela está vazia porque as propriedades são removidas na transpilação para JavaScript puro.
class CreateItemDto {
}
exports.CreateItemDto = CreateItemDto;

//# sourceMappingURL=create-item.dto.js.map
// Comentário gerado pelo TypeScript para mapear o código-fonte original (create-item.dto.ts) ao arquivo transpilado (create-item.dto.js).