"use strict"; // Ativa o modo estrito do JavaScript para evitar erros comuns.

// Define a propriedade __esModule para compatibilidade com módulos ES6.
// Isso garante que o arquivo pode ser importado corretamente em outros módulos.
Object.defineProperty(exports, "__esModule", { value: true });

// Exporta a classe UpdateItemDto para ser usada em outros arquivos.
exports.UpdateItemDto = void 0;

// Define a classe UpdateItemDto, que representa o Data Transfer Object (DTO) para atualização de itens.
// No TypeScript original, essa classe define os campos esperados ao atualizar um item.
// Aqui, ela está vazia porque as propriedades são removidas na transpilação para JavaScript puro.
class UpdateItemDto {
}
exports.UpdateItemDto = UpdateItemDto;

//# sourceMappingURL=update-item.dto.js.map
// Comentário gerado pelo TypeScript para mapear o código-fonte original (update-item.dto.ts) ao arquivo transpilado (update-item.dto.js).